{
  "name": "cloud-resume-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:test-env": "TEST_ENV=test playwright test",
    "test:prod-env": "TEST_ENV=prod playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "lodash": "^4.17.15",
    "typescript": "^5.3.3"
  }
}
